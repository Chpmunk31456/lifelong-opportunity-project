name: Guide 78 validator correction audit

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - name: Check out controlled revision branch
        uses: actions/checkout@v5
        with:
          ref: revision/guide-00-100-2026
      - name: Record retired correction path
        run: |
          echo 'The workflow-mutating correction path is retired.'
          echo 'Guide 78 duration wording is normalized in the controlled English master; publication remains fail-closed.'
